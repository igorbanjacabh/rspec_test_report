<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:test_runs, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:test_runs, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:test_run) %></h2>
    <div class="inner">
      <% form_for :test_run, url(:test_runs, :create), :class => :form do |f| %>
        <%= partial "test_runs/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>