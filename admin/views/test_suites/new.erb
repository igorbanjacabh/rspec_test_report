<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:test_suites, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:test_suites, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:test_suite) %></h2>
    <div class="inner">
      <% form_for :test_suite, url(:test_suites, :create), :class => :form do |f| %>
        <%= partial "test_suites/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>